type MtNFT @entity {
  id: ID!
  url: String
  owner: Bytes! # address
  approved: Bytes # address
}

type MtTransfer @entity {
  id: ID!
  tokenId: String!
  from: Bytes! # address
  to: Bytes! # address
}
